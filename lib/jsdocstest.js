/**
 * Fab.
 * 
 * @property {Number} maxX - The maximum X position.
 * @property {Object} state - test object
 * @property {Number} state.x - x position
 */
class Fab {
    constructor() {
        this.maxX = null
    }

    /**
 * Move to an XYZ position. This is a longer description because I'm interestred in what the output will look like.
 * @param {Number} - x position
 * @param {Number} - y position
 * @param {Number} - z position
 * 
 * @example
 * 
 * moveTo(1, 2, 3);
 */
    moveTo(x, y, z) { }
    /**
     * Move relative in X.
     * @param {Number} - x position
     * 
     * @example
     * 
     * moveX(10);
     */
    moveX(dx) { }
}